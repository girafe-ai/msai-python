# Write a function that takes a list of arbitrary elements and returns a list of
# unique objects (elements that are in different memory locations)


def unique(seq):
    """
    :param seq: List of arbitrary elements
    :return List: unique elements in seq with different ids
    """
    # Write your code here
    ...

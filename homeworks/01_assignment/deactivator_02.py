# Implement class Deactivator, which initialises by boolean is_active parameter with True value by default
# Class Deactivator must have is_active() method which returns True if instance is active
# Class Deactivator must have deactivate_all() method which deactivate all existed instances
# You can enrich Deactivator class as much as you want


class Deactivator:

    def is_active(self):
        """ Displays if instance of `Deactivator` class is active """
        ...

    def deactivate_all(self):
        """ Deactivates all instances of `Deactivator` class """
        ...
